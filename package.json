{
  "name": "clone-tabnews",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "dependencies": {
    "next": "^13.1.6",
    "pg": "^8.11.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "author": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run service:up && next dev",
    "service:up": "docker compose -f infra/compose.yml up -d",
    "service:stop": "docker compose -f infra/compose.yml stop",
    "service:down": "docker compose -f infra/compose.yml down",
    "service:build": "docker compose -f infra/compose.yml up -d --build",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "prettier": "^3.6.2"
  }
}
